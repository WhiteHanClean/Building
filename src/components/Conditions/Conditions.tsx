import React from 'react';
import s from "@/components/Conditions/Conditions"

const Conditions: React.FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default Conditions;